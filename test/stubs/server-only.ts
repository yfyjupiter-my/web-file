// Test stub for Next.js's `server-only` marker package. In the app this import
// makes a module hard-fail if pulled into a client bundle; under Vitest there's
// no bundler boundary, so it resolves to this empty module.
export {};
